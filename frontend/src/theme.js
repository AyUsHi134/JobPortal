import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#7b42f6',
    },
    secondary: {
      main: '#5f43b2',
    },
    background: {
      default: "#f7f3fd",
    }
  }
});

export default theme;
